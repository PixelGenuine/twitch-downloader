let methods = {
    /**
     * Report errors to the Twitter bot @ErrorsTracker
     * All errors are public info and freely accessable
     */
    report: function(error) {
        /**
         * This function will be built when Twitter approves
         * the API application for @ErrorsTracker.
         * I can't do anything until then.
         */
    }
}

module.exports = methods